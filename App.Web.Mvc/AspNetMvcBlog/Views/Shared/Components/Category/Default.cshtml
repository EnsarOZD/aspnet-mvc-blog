

<div class="dropdown-menu" aria-labelledby="navbarDropdown3">
	
	@foreach (var item in Model)

	{
		<a class="dropdown-item" asp-controller="Category" asp-action="@item.Action">@item.Name</a>
	}
</div>